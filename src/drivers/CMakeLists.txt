add_subdirectory(irm)
if (BINARY_DIR_NAME STREQUAL "lotus")
  add_subdirectory(lotus-alias)
endif()
if (BINARY_DIR_NAME STREQUAL "cclyzerpp")
  add_subdirectory(cclyzerpp)
endif()
if (BINARY_DIR_NAME STREQUAL "svf")
  add_subdirectory(svf-wpa)
endif()
